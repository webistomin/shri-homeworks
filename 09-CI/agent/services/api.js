const fsPath = require('path');
const cp = require('child_process');
const util = require('util');
const asyncExec = util.promisify(cp.exec);

module.exports = class API {
  
  constructor() {
    const agentStatus = 'fsd'
  }
  
  
  
};
